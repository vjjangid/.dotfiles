# **Issue Log for mac nvim setup**
1. For omnisharp mac issues
    - check whether dotnet installe and dotnet path is 
    
    ```
    export DOTNET_ROOT=/usr/local/share/dotnet

    export PATH="$DOTNET_ROOT:$PATH"
    ```
